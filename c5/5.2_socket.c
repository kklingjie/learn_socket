#include <sys/socket.h>
#include <sys/types.h>

/**
 * [socket description]
 * @param  domain   [PF_INET protocol family of internet]
 * @param  type     [SOCK_UGRAM / SOCK_STRAM]
 * @param  protocol [0 默认协议]
 * @return fd / -1
 */
int socket(int domain,int type, int protocol)

int main(int argc, char const *argv[]) {
    
    return 0;
}
